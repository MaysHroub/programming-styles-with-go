package config

// InputFile is the path to the input text file.
const InputFile = "files/input.txt"

// StopWordsFile is the path to the stop words file.
const StopWordsFile = "files/stopwords.txt"

// This is for ch25: persistent tables
const PathToDB = "../sql/schema/testdb.db"
